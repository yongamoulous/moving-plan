# Off the grid — 오프라인 기내 P2P 네트워킹 서비스

> **MVP 상세 기획서 (PRD)**
> 문서 버전 v0.1 · 최종 수정 2026-06-04 · 작성: Product
>
> _이 문서는 AI Studio / 개발 연동 시 컨텍스트로 그대로 붙여넣어 사용하는 것을 목적으로 한다._

---

## 0. TL;DR

인터넷이 단절된 비행기 기내에서 **BLE + Wi-Fi Direct 하이브리드 P2P 메쉬**로 탑승객 간
대화와 매칭을 제공하고, **착륙과 동시에 모든 데이터가 사라지는(Gone on arrival)**
오프라인 네트워킹 앱.

- **두 개의 트랙:** (A) 좌석이 떨어진 일행과의 PIN 기반 비공개 대화, (B) 같은 비행편 승객과의 익명 블라인드 매칭.
- **핵심 차별점:** 서버 없는 P2P, 종단간 암호화(E2EE), TTL 기반 자동 파기.
- **MVP 목표:** "일행과 대화(A존)" 유틸리티로 콜드스타트를 돌파하고, 같은 기내에서 2명 이상이 실제로 연결되는 경험을 검증한다.

---

## 1. 프로덕트 개요 (Product Overview)

| 항목 | 내용 |
|---|---|
| **서비스 정의** | 인터넷이 단절된 기내 환경에서 블루투스와 Wi-Fi Direct를 활용해 탑승객 간 끊김 없는 대화와 새로운 매칭을 제공하는 오프라인 P2P 네트워킹 앱 |
| **메인 슬로건** | _"Off the grid. Gone on arrival."_ — 망에서 벗어나 안전하게 대화하고, 도착과 동시에 흔적 없이 사라진다 |
| **플랫폼 (MVP)** | iOS / Android 네이티브 (P2P·BLE 권한 제약상 네이티브 필수) |
| **핵심 제약** | 인터넷 없음, 서버 호출 없음(기내), 배터리 한정, 비행 시간이라는 명확한 TTL |

### 1.1 타겟 유저 (MVP)

1. **동반 여행객** — 일행과 좌석이 떨어져 배정된 2인 이상 그룹 _(핵심 콜드스타트 진입점)_
2. **2030 솔로 탑승객** — 장시간 비행의 지루함을 달래고 싶고 새로운 네트워킹에 열려 있는 사람

### 1.2 해결하려는 문제 (Problem)

- 기내 Wi-Fi는 비싸거나 없고, 셀룰러는 차단 → **일행과 떨어지면 연락 수단이 없다.**
- 장거리 비행의 무료함, 옆/주변 승객과 자연스럽게 연결될 수단의 부재.
- 기존 데이팅/네트워킹 앱은 모두 **온라인·서버 의존**이라 기내에서 동작하지 않는다.

---

## 2. 목표 및 성공 지표 (Goals & Success Metrics)

### 2.1 MVP 가설

- **H1 (유틸리티):** 일행과 떨어진 그룹은 기내에서 대화할 수단을 절실히 원하며, 이를 위해 탑승 전 함께 앱을 설치한다.
- **H2 (네트워킹):** A존 사용자 중 일부는 B존(블라인드 매칭)으로 자연스럽게 넘어간다.

### 2.2 North Star Metric

> **"한 비행편에서 실제로 메시지를 주고받은 P2P 페어 수"** (Connected Pairs per Flight)

### 2.3 핵심 지표 (KPI)

| 구분 | 지표 | MVP 기준선(가설) |
|---|---|---|
| 획득 | 초대 수락률 (동반자 초대 루프) | 초대 1건당 ≥ 0.6 설치 |
| 활성 | A존 채팅방 생성률 | 그룹 설치 중 ≥ 70% |
| 전환 | A존 → B존 레이더 On 전환율 | ≥ 25% |
| 매칭 | 더블 옵트인 매칭 성사율 | Like 발신 중 ≥ 15% 상호수락 |
| 신뢰 | TTL 파기 후 데이터 잔존 0건 | 100% (보안 필수) |
| 리텐션 | 동일 사용자의 2번째 비행 재사용률 | ≥ 20% |

---

## 3. 범위 (Scope)

### 3.1 In Scope (MVP)

- 온라인 온보딩 + 인증 배지(JWT) 사전 발급
- 비행기 모드 감지 기반 메인 진입
- **A존:** PIN 기반 1:N 비공개 그룹 채팅 (텍스트)
- **B존:** RSSI 기반 레이더 UI + 더블 옵트인 매칭 + 1:1 비밀 채팅
- BLE 탐색 + Wi-Fi Direct 데이터 전송 하이브리드 메쉬
- E2EE (A: 대칭키 / B: 비대칭키)
- TTL 기반 세션 타이머 및 로컬 DB 완전 삭제

### 3.2 Out of Scope (MVP 제외, 추후)

- 음성/영상 통화, 파일·이미지 전송 (텍스트 우선)
- 기내 외(공항·라운지) 매칭
- 결제/구독, 프리미엄 기능
- 신고/모더레이션 백오피스(최소한의 차단만)
- 다국어 (한국어/영어 우선)
- 크로스 플랫폼 메쉬 상호운용 고도화(iOS↔Android는 검증 항목)

---

## 4. 핵심 아키텍처 및 기술 스펙 (Tech Specs)

### 4.1 통신 프로토콜

- **하이브리드 P2P 메쉬:** **BLE(탐색/시그널링)** + **Wi-Fi Direct(대용량·고속 데이터 전송)**.
- BLE Advertising으로 주변 노드 탐색 → Wi-Fi Direct 채널 협상 → 데이터 채널 수립.
- 멀티홉 중계로 직접 도달이 안 되는 노드까지 메시지 전파(메쉬).

### 4.2 데이터 분산 저장 (RAID 유사 구조)

- **CRDT**(Conflict-free Replicated Data Type) 기반으로 노드 간 충돌 없이 상태를 병합.
- 연결된 N개 노드가 **암호화된 데이터 블록을 분산 보관·중계**하여, 일부 노드가 이탈해도 메시지 가용성 확보.
- 오프라인·단속적 연결 환경을 전제로 한 **eventual consistency** 모델.

### 4.3 보안 및 암호화 (E2EE)

| 트랙 | 키 교환 | 방식 |
|---|---|---|
| A존 (지인/난수 방) | 대칭키 | PIN/QR에서 파생된 공유키로 터널링 |
| B존 (블라인드 매칭) | 비대칭키 | 공개키/개인키 교환을 통한 종단간 암호화 |

- 인증 배지(JWT)는 **온라인 단계에서 서버 발급** → 기내에서는 검증만(오프라인 서명 검증).
- 메시지는 종단간 암호화되며, 중계 노드는 암호문만 보관(내용 비가시).

### 4.4 배터리 스로틀링 (Battery Throttling)

- 화면 On/Off 상태에 따라 BLE 스캐닝 주기 동적 조절.
- 배터리 잔량 **20% 이하 시 저전력 모드**(스캔 간격 확대, 광고 주기 완화).
- 포그라운드/백그라운드 듀티사이클 분리.

### 4.5 데이터 자동 파기 (TTL)

- 비행 잔여 시간에 맞춘 **세션 타이머** 작동.
- 만료 시 기기 내 로컬 DB(SQLite) **완전 삭제** → "Gone on arrival" 구현.
- 트리거: (1) TTL 만료, (2) 비행기 모드 해제(착륙 추정), (3) 수동 즉시 파기.

### 4.6 로컬 데이터 모델 (초안)

```
Profile      { id, badgeJWT(검증용), nickname, interestTags[], avatarBlob(잠금) }
Session      { flightCode, startedAt, ttlExpiresAt, mode(A|B) }
Room(A)      { roomId, pinHash, sharedKey, members[], createdAt }
Match(B)     { matchId, theirPubKey, status(liked|accepted), unlockedAt }
Message      { id, scope(roomId|matchId), ciphertext, sentAt, vectorClock }  // CRDT
```

> 모든 테이블은 TTL 만료 시 drop. 영구 식별자(전화번호 등)는 로컬에 저장하지 않는다.

---

## 5. 사용자 여정 및 UI/UX 구조 (User Journey)

### Step 1 — 온보딩 및 사전 검증 (탑승 전 / 온라인)

- 소셜 로그인(Instagram, Google 등)으로 정량적 신뢰 데이터 수집.
- 서버가 **암호화된 인증 배지(JWT)** 를 발급 → 로컬 기기에 저장(기내에서 오프라인 검증).
- Micro-copy: _"착륙과 동시에 모든 로컬 데이터는 완벽하게 파기됩니다."_ (전환율/uCTR 극대화)

### Step 2 — 메인 홈 (기내 / 오프라인)

- **비행기 모드 강제:** 비행기 모드 활성화 시에만 다크 모드 기반 메인 UI 진입.
- **상단 Status Bar:** 시각적 TTL 타이머 노출 — _"착륙까지 약 4시간 15분 남음"_.
- **Two-Track 라우팅:**
  - **[A존] 일행과 대화** — 4자리 PIN 입력 시 프로필 노출 없이 즉시 암호화 채팅방 생성.
  - **[B존] 새로운 인연 탐색** — 레이더 스위치 On 시 내 익명 프로필 브로드캐스팅 시작.

### Step 3 — 탐색 및 매칭 (B존 진입 시)

- **거리감 기반 레이더 UI:** BLE 신호 세기(RSSI)로 주변 유저를 모호한 실루엣+텍스트로 표현
  (예: _"조금 먼 좌석의 인스타 1K+ 유저"_).
- **더블 옵트인 (Double Opt-in):**
  1. **1차** — 신뢰도 배지 + 관심사 태그만 확인 후 '호감(Like)' 전송.
  2. **2차** — 상호 수락(Accept) 시 비대칭키 교환 완료 → 프로필 사진 잠금 해제 + 1:1 비밀 채팅 활성화.

### 5.1 화면 목록 (Wireframe 도출용)

1. 스플래시 / 로그인
2. 소셜 연동 & 배지 발급 완료
3. 비행편 등록(예: KE701) + 보딩패스 UI
4. (비행기 모드 OFF) 안내 게이트
5. 메인 홈 — TTL 바 + A/B 라우팅
6. A존: PIN 입력 → 그룹 채팅방
7. B존: 레이더 스캔 화면
8. B존: 상대 카드(배지·태그) → Like
9. B존: 매칭 성사 → 1:1 채팅
10. 세션 종료 / 데이터 파기 확인

---

## 6. 콜드스타트 및 그로스 전략 (Growth Strategy)

- **동반자 강제 초대 루프:** '오프라인 지인 통신(A존)'이라는 확실한 유틸리티를 미끼로,
  탑승 전 일행 간 자발적 설치 및 P2P 노드 확보 유도.
- **FOMO 자극 (탑승 게이트 바이럴):** 비행편 등록 시 생성되는 힙한 보딩패스 UI를
  인스타그램 스토리에 공유 → 동일 비행편 승객의 호기심 유발.
- **Shoulder-Surfing 유도:** 다크 모드 + 네온 액센트 + 역동적 레이더 애니메이션으로
  기내에서 타인의 시선을 끄는 시각적 디자인.

---

## 7. 마일스톤 (Roadmap)

| 단계 | 범위 | 검증 목표 |
|---|---|---|
| **M0 — 기술 PoC** | BLE 탐색 + Wi-Fi Direct 1:1 연결, iOS↔Android 상호운용 | 두 기기가 오프라인으로 텍스트 교환 |
| **M1 — A존 MVP** | PIN 그룹 채팅 + 대칭키 E2EE + TTL 파기 | 그룹이 기내에서 실제 대화 |
| **M2 — B존 MVP** | 레이더 UI + 더블 옵트인 + 비대칭키 + 메쉬 중계 | 모르는 승객과 매칭·대화 |
| **M3 — 그로스** | 보딩패스 공유, 초대 루프, 분석 계측 | North Star(Connected Pairs) 측정 |

---

## 8. 리스크 및 대응 (Risks)

| 리스크 | 영향 | 대응 |
|---|---|---|
| iOS의 백그라운드 BLE/멀티피어 제약 | 탐색·중계 불안정 | iOS MultipeerConnectivity 검토, 포그라운드 사용 가정 |
| iOS↔Android 메쉬 상호운용 난이도 | 동일 비행편 분절 | M0에서 최우선 검증, 실패 시 동일 OS 우선 |
| 항공사 기기/규정(전파) 이슈 | 사용 제약 | 비행기 모드 내 BLE/Wi-Fi 허용 범위 확인 |
| 콜드스타트(동일 비행편 밀도 부족) | B존 공허 | A존 유틸리티 우선, 그룹 단위 침투 |
| 안전/괴롭힘 이슈 | 신뢰 훼손 | 차단 기능, 배지 기반 신뢰, TTL로 흔적 최소화 |
| 배터리 소모 | 이탈 | 스로틀링·듀티사이클, 저전력 모드 |

---

## 9. 미해결 질문 (Open Questions)

- 비행 시간(TTL)은 어떻게 산정하나? 사용자 입력 vs 비행편 DB(온라인 단계) vs 수동.
- "인스타 1K+" 같은 신뢰 시그널의 검증 범위와 프라이버시 경계는?
- 멀티홉 중계 시 중계 노드의 배터리 기여를 어떻게 보상/완화하나?
- 매칭 안전장치(미성년자/괴롭힘) 최소 요건은?
- 동일 비행편 식별을 사용자 입력에 의존할 때의 스푸핑 방지책은?

---

_End of PRD v0.1_
